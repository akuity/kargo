export const UseDigestTag = ({ visible }: { visible?: boolean }) =>
  visible && (
    <div className='text-xs text-white font-medium bg-gray-400 rounded px-2 py-1 mr-4'>
      USE DIGEST
    </div>
  );
