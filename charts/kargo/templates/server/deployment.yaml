{{- $tlsCert := "" }}
{{- $tlsKey := "" }}
{{- if .Values.server.tls.enabled }} 
  {{- if .Values.server.tls.generateSelfSignedCert }}
    {{- $data := (lookup "v1" "Secret" .Release.Namespace (printf "%s-cert" (include "kargo.server.fullname" . ))).data }}
    {{- if $data }}
      # Reuse the existing cert and key
      {{- $tlsCert = index $data "tls.crt" }}
      {{- $tlsKey = index $data "tls.key" }}
    {{- else }}
      # Generate a new cert and key
      {{- $altName1 := include "kargo.server.fullname" . }}
      {{- $altName2 := printf "%s.%s" (include "kargo.server.fullname" .) .Release.Namespace }}
      {{- $altName3 := printf "%s.%s.svc" (include "kargo.server.fullname" .) .Release.Namespace }}
      {{- $altName4 := printf "%s.%s.svc.cluster" (include "kargo.server.fullname" .) .Release.Namespace }}
      {{- $altName5 := printf "%s.%s.svc.cluster.local" (include "kargo.server.fullname" .) .Release.Namespace }}
      {{- $cert := genSelfSignedCert .Values.server.host nil (list .Values.server.host $altName1 $altName2 $altName3 $altName4 $altName5) 3650 }}
      {{- $tlsCert = b64enc $cert.Cert }}
      {{- $tlsKey = b64enc $cert.Key }}
    {{- end }}
  {{- else if .Values.server.tls.cert }}
    # Use operator supplied cert and key
    {{- $tlsCert = .Values.server.tls.cert }}
    {{- $tlsKey = .Values.server.tls.key }}
  {{- end }}
{{- end }}
---
{{- if and .Values.server.tls.enabled (or .Values.server.tls.generateSelfSignedCert .Values.server.tls.cert) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kargo.server.fullname" . }}-cert
  labels:
    {{- include "kargo.labels" . | nindent 4 }}
    {{- include "kargo.server.labels" . | nindent 4 }}
type: Opaque
data:
  tls.crt: {{ $tlsCert }}
  tls.key: {{ $tlsKey }}
{{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kargo.server.fullname" . }}
  labels:
    {{- include "kargo.labels" . | nindent 4 }}
    {{- include "kargo.server.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicas }}
  selector:
    matchLabels:
      {{- include "kargo.selectorLabels" . | nindent 6 }}
      {{- include "kargo.server.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kargo.selectorLabels" . | nindent 8 }}
        {{- include "kargo.server.labels" . | nindent 8 }}
      annotations:
        {{- if and .Values.server.tls.enabled (or .Values.server.tls.generateSelfSignedCert .Values.server.tls.cert) }}
        checksum/tls-cert: {{ sha256sum $tlsCert }}
        checksum/tls-key: {{ sha256sum $tlsKey }}
        {{- end }}
    spec:
      serviceAccount: {{ include "kargo.server.fullname" . }}
      imagePullSecrets:
      - name: {{ include "kargo.fullname" . }}-image-pull-secret
      containers:
      - name: server
        image: {{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: ["/usr/local/bin/kargo-server"]
        env:
        - name: LOG_LEVEL
          value: {{ .Values.server.logLevel }}
        - name: TLS_ENABLED
          value: {{ quote .Values.server.tls.enabled }}
        {{- if .Values.server.tls.enabled }}
        - name: TLS_CERT_PATH
          value: /var/kargo/certs/tls.crt
        - name: TLS_KEY_PATH
          value: /var/kargo/certs/tls.key
        {{- end }}
        {{- if .Values.server.tls.enabled }}
        volumeMounts:
        - name: cert
          mountPath: /var/kargo/certs
          readOnly: true
        {{- end }}
        livenessProbe:
          httpGet:
            port: 8080
            path: /healthz
            {{- if .Values.server.tls.enabled }}
            scheme: HTTPS
            {{- end }}
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            port: 8080
            path: /healthz
            {{- if .Values.server.tls.enabled }}
            scheme: HTTPS
            {{- end }}
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          {{- toYaml .Values.server.resources | nindent 10 }}
      {{- if .Values.server.tls.enabled }}
      volumes:
      - name: cert
        secret:
          secretName: {{ include "kargo.server.fullname" . }}-cert
      {{- end }}
      {{- with .Values.server.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
