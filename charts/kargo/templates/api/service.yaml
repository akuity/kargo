{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: kargo-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kargo.labels" . | nindent 4 }}
    {{- include "kargo.api.labels" . | nindent 4 }}
  {{- include "kargo.annotations" (dict "root" . "annotations" .Values.api.service.annotations) | nindent 2 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
  - protocol: TCP
    {{- if .Values.api.tls.enabled }}
    port: 443
    {{- else }}
    port: 80
    {{- end }}
    {{- if and (or (eq .Values.api.service.type "NodePort") (eq .Values.api.service.type "LoadBalancer")) .Values.api.service.nodePort}}
    nodePort: {{ .Values.api.service.nodePort }}
    {{- end }}
    targetPort: 8080
  selector:
    {{- include "kargo.selectorLabels" . | nindent 4 }}
    {{- include "kargo.api.labels" . | nindent 4 }}
{{- end }}
