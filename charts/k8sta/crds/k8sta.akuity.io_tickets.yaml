---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: tickets.k8sta.akuity.io
spec:
  group: k8sta.akuity.io
  names:
    kind: Ticket
    listKind: TicketList
    plural: tickets
    singular: ticket
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Ticket is the Schema for the tickets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TicketSpec defines the desired state of Ticket
            properties:
              change:
                description: Change encapsulates the specific change this Ticket is
                  meant to progress through a series of environments.
                properties:
                  imageRepo:
                    description: ImageRepo denotes a new image (without tag) that
                      is to be progressed through a series of environments. The value
                      of this field only has meaning when the value of the Type field
                      is "NewImage".
                    type: string
                  imageTag:
                    description: ImageTag qualifies which image from the repository
                      specified by the ImageRepo filed is to be progressed through
                      a series of environments. The value of this field only has meaning
                      when the value of the Type field is "NewImage".
                    type: string
                  type:
                    description: Type indicates a class of change that needs to be
                      progressed through a series of environments. The controller
                      knows how to deal with different classes of change based on
                      the value of this field.
                    type: string
                type: object
              line:
                description: Line is a reference to a K8sTA Line.
                type: string
              source:
                description: Source indicates how this ticket entered the system.
                type: string
            type: object
          status:
            description: TicketStatus defines the observed state of Ticket
            properties:
              state:
                description: TicketState represents the overall state of the Ticket.
                type: string
              stateReason:
                description: StateReason provides context for why the Ticket is in
                  the State that it is.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
