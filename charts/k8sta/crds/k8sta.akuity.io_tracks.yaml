---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: tracks.k8sta.akuity.io
spec:
  group: k8sta.akuity.io
  names:
    kind: Track
    listKind: TrackList
    plural: tracks
    singular: track
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Track is the Schema for the tracks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          environments:
            description: Environments enumerates a logical ordering of environments
              through which a change represented by a Ticket should be progressed.
            items:
              description: Environment represents a single environment through which
                a change represented by a Ticket should be progressed.
              properties:
                application:
                  description: ArgoCDApplication is a reference to an existing Argo
                    CD Application resource that managed deployments to this Environment.
                  type: string
                name:
                  description: Name is a name for the environment.
                  type: string
              type: object
            type: array
          imageRepositories:
            description: ImageRepositories specifies image repositories that the Track
              is effectively subscribed to. When a push to any one of these repositories
              is detected, it will trigger the progressive deployment of the new image
              through the Track's environments.
            items:
              type: string
            type: array
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
        required:
        - imageRepositories
        type: object
    served: true
    storage: true
