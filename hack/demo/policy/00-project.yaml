apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: policy-demo
  labels:
    # Custom policies can key off Project labels -- the operator's PCI rule
    # (40-clusterconfig.yaml) matches this one.
    compliance: pci
    team: payments
