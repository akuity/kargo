apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: policy-demo
  labels:
    # Projected into policy input as input.project.labels (annotations
    # likewise), so custom policies can be data-driven.
    compliance: pci
    team: payments
