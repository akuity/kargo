apiVersion: kargo.akuity.io/v1alpha1
kind: ProjectConfig
metadata:
  name: policy-demo
  namespace: policy-demo
spec:
  promotionPolicies:
  - stageSelector:
      name: test
    autoPromotionEnabled: true
  - stageSelector:
      name: uat
    autoPromotionEnabled: true

  # Promotion windows: forward promotions to a governed Stage dispatch only
  # while a window is open. Held Promotions stay Pending and dispatch on
  # their own when the window opens.
  promotionWindows:
  - name: uat-evenings
    stageSelector:
      name: uat
    recurrence: FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR
    start: "18:00"
    end: "23:00"
    location: America/Los_Angeles

  # Rolling-window rate limit: at most maxPromotions automatic dispatches
  # to a matching Stage within any trailing window.
  rateLimits:
  - name: test-throttle
    stageSelector:
      name: test
    maxPromotions: 1
    window: 2m

  # Rules only -- the kargo.project package and stdlib imports are
  # prepended. See pkg/promotion/dispatch/policy/README.md.
  #
  # customPolicy: |
  #   violation contains v if {
  #   	input.stage.name == "prod"
  #   	input.promotion.class == "manual-forward"
  #   	not input.promotion.annotations["approved-by"]
  #   	v := {
  #   		"rule": "prod-approval",
  #   		"msg": "prod promotions require an approved-by annotation",
  #   	}
  #   }
