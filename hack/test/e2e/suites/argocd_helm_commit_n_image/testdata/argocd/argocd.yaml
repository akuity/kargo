apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kargo-argocd-helm-commit-image
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - stage: test
      - stage: uat
      - stage: prod
  template:
    metadata:
      name: kargo-argocd-helm-commit-image-{{stage}}
      annotations:
        kargo.akuity.io/authorized-stage: kargo-argocd-helm-commit-image:{{stage}}
    spec:
      project: default
      source:
        repoURL: https://github.com/<github-username>/kargo-demo-gitops.git
        # Kargo will update targetRevision to move new commits from the
        # new-helm branch into each stage
        targetRevision: placeholder
        path: charts/kargo-demo
        helm:
          parameters:
          # Kargo will update this value to move new images into each stage
          - name: image.name
            value: public.ecr.aws/nginx/nginx:placeholder
      destination:
        server: https://kubernetes.default.svc
        namespace: kargo-argocd-helm-commit-image-{{stage}}
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
  ignoreApplicationDifferences:
  - jqPathExpressions:
    - .spec.source.targetRevision
    - .spec.source.helm.parameters
