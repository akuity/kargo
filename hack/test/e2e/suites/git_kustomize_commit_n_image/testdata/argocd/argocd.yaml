apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kargo-git-kustomize-commit-image
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - stage: test
      - stage: uat
      - stage: prod
  template:
    metadata:
      name: kargo-git-kustomize-commit-image-{{stage}}
      annotations:
        kargo.akuity.io/authorized-stage: kargo-git-kustomize-commit-image:{{stage}}
    spec:
      project: default
      source:
        repoURL: https://github.com/<github-username>/kargo-demo-gitops.git
        # Kargo will update this branch to move new commits from the kustomize
        # branch and/or new images into this stage
        targetRevision: placeholder
        path: .
      destination:
        server: https://kubernetes.default.svc
        namespace: kargo-git-kustomize-commit-image-{{stage}}
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
  ignoreApplicationDifferences:
  - jqPathExpressions:
    - .spec.source.targetRevision