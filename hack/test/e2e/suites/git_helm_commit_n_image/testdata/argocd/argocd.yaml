apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kargo-git-helm-commit-image
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - stage: test
      - stage: uat
      - stage: prod
  template:
    metadata:
      name: kargo-git-helm-commit-image-{{stage}}
      annotations:
        kargo.akuity.io/authorized-stage: kargo-git-helm-commit-image:{{stage}}
    spec:
      project: default
      source:
        repoURL: https://github.com/<github-username>/kargo-demo-gitops.git
        # Kargo will update this branch to move new commits from the helm branch
        # and/or new images into this stage
        targetRevision: helm-commit-image/stage/{{stage}}
        path: ./kargo-demo/templates
      destination:
        server: https://kubernetes.default.svc
        namespace: kargo-git-helm-commit-image-{{stage}}
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
